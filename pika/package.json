{
    "scripts": {
        "web": "npx @pika/web --dest ../docs/web_modules",
        "build": "npm install && npm run web"
    },
    "dependencies": {
        "@wonkytech/tm-examples": "latest"
    }
}
